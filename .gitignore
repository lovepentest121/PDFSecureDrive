# NEVER commit these — secrets / credentials
google-services.json
*.jks
*.keystore
# Allow fixed debug keystore (no secrets — only for consistent SHA-1 in CI)
!keystore/debug.keystore
local.properties
*.env

# Build outputs
/build
/app/build
/.gradle
/.idea
*.iml
.DS_Store

# Gradle caches
.gradle/

# APK outputs
*.apk
*.aab
